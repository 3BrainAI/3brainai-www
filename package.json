{
  "name": "3brainai-www-review",
  "private": true,
  "version": "0.0.0",
  "description": "Deterministic review checks for the static 3BrainAI website.",
  "engines": {
    "node": ">=24.8.0 <25"
  },
  "scripts": {
    "validate": "npm run validate:html && npm run validate:css && npm run validate:ai",
    "validate:html": "html-validate --max-warnings=0 \"**/*.html\"",
    "validate:css": "csstree-validator assets/css/style.css",
    "validate:ai": "node scripts/validate-ai-visibility.mjs",
    "test:r3": "playwright test",
    "indexnow": "node scripts/submit-indexnow.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "csstree-validator": "4.0.1",
    "html-validate": "11.6.1",
    "http-server": "14.1.1"
  }
}
